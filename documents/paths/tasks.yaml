get:
  summary: List all tasks
  tags:
    - Tasks
  parameters:
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/Task.yaml
    '401':
      $ref: ../components/responses/Unauthenticated.yaml

